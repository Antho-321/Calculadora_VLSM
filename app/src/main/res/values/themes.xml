<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.Calculadora_VLSM" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Personaliza tu tema de luz aquí -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:windowBackground">@color/background</item>

        <!-- Agrega otras personalizaciones de Material Design si es necesario -->
    </style>

    <!-- Tema principal de la aplicación -->
    <style name="Theme.Calculadora_VLSM" parent="Base.Theme.Calculadora_VLSM" />
</resources>
